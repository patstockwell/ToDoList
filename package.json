{
  "name": "todolist",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-css": "node-sass --include-path scss dev/scss/main.scss public/css/main.css",
    "watch-css": "nodemon -e scss -x \"npm run build-css\"",
    "reload": "browser-sync start --server --files \"public/css/*.css\" \"*.html\""
  },
  "author": "Patrick Stockwell",
  "license": "ISC",
  "devDependencies": {
  },
  "dependencies": {
    "mustache": "^2.3.0"
  }
}
